# 🏗️ Deployment Architecture

## Visual Guide to Your Portfolio Deployment

---

## 🎯 Option 1: Railway (All-in-One) - RECOMMENDED

```
┌─────────────────────────────────────────────────────────────┐
│                        RAILWAY.APP                          │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Your Portfolio Application             │   │
│  │                                                     │   │
│  │  Frontend (HTML/CSS/JS)                            │   │
│  │  ├── index.html (Hub)                              │   │
│  │  ├── wezeso.html                                   │   │
│  │  ├── sardor/alikhan/amirkhan.html                  │   │
│  │  └── styles.css + script.js                        │   │
│  │                                                     │   │
│  │  Backend (Node.js/Express)                         │   │
│  │  ├── /api/auth                                     │   │
│  │  ├── /api/projects                                 │   │
│  │  ├── /api/team                                     │   │
│  │  ├── /api/admin                                    │   │
│  │  ├── /login (page)                                 │   │
│  │  └── /admin (page)                                 │   │
│  │                                                     │   │
│  └──────────────────┬──────────────────────────────────┘   │
│                     │                                       │
│                     ├─────────────────┐                     │
│                     ↓                 ↓                     │
│  ┌──────────────────────────┐  ┌──────────────────────┐   │
│  │  PostgreSQL Database     │  │   File Storage       │   │
│  │  ├── users               │  │   └── /uploads/      │   │
│  │  ├── team_members        │  │                      │   │
│  │  ├── projects            │  │   (or Cloudinary)    │   │
│  │  └── sessions            │  │                      │   │
│  └──────────────────────────┘  └──────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                             │
                             ↓
                    https://your-app.railway.app
                    (Custom domain optional)
```

**Pros:**
- ✅ Everything in one place
- ✅ Easy to manage
- ✅ Auto-scaling
- ✅ Built-in monitoring

**Best for:** Most users, quick deployment

---

## 🎯 Option 2: Split Stack (Optimized)

```
┌──────────────────────────────────────────────────────────────┐
│                         VERCEL                               │
│  ┌────────────────────────────────────────────────────────┐  │
│  │               Static Frontend                          │  │
│  │  ├── HTML/CSS/JS (All portfolio pages)                │  │
│  │  └── Served via Global CDN                            │  │
│  └──────────────────────┬─────────────────────────────────┘  │
└─────────────────────────┼────────────────────────────────────┘
                          │
                          │ API Calls
                          ↓
┌──────────────────────────────────────────────────────────────┐
│                         RAILWAY                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │               Backend API                              │  │
│  │  ├── /api/auth                                         │  │
│  │  ├── /api/projects                                     │  │
│  │  ├── /api/team                                         │  │
│  │  └── /api/admin                                        │  │
│  └──────────────────────┬─────────────────────────────────┘  │
│                         │                                     │
│                         ↓                                     │
│  ┌────────────────────────────────────────────────────────┐  │
│  │               PostgreSQL Database                      │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
                          │
                          ↓
         ┌────────────────────────────────┐
         │         CLOUDINARY             │
         │    (File Storage - Optional)   │
         └────────────────────────────────┘
```

**Pros:**
- ✅ Best frontend performance (CDN)
- ✅ Separate scaling
- ✅ Both have free tiers

**Best for:** High traffic, performance optimization

---

## 🎯 Option 3: VPS (Full Control)

```
┌──────────────────────────────────────────────────────────────┐
│              DIGITALOCEAN DROPLET (Ubuntu)                   │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │                      NGINX                             │  │
│  │              (Reverse Proxy + SSL)                     │  │
│  └───┬──────────────────────────────────────────────────┬─┘  │
│      │                                                  │    │
│      │ Port 80/443                         Static Files │    │
│      ↓                                                  ↓    │
│  ┌────────────────────────────────────┐  ┌──────────────┐   │
│  │         Node.js (PM2)              │  │  /var/www/   │   │
│  │  ┌──────────────────────────────┐  │  │  └── Frontend│   │
│  │  │  Express Server (Port 3000)  │  │  └──────────────┘   │
│  │  │  ├── API Routes              │  │                     │
│  │  │  ├── Auth                    │  │  ┌──────────────┐   │
│  │  │  └── Static Serving          │  │  │   /uploads/  │   │
│  │  └──────────────┬───────────────┘  │  └──────────────┘   │
│  └─────────────────┼────────────────────┘                    │
│                    │                                         │
│                    ↓                                         │
│  ┌────────────────────────────────────────────────────────┐  │
│  │              PostgreSQL (Local)                        │  │
│  │              Port 5432                                 │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
└──────────────────────────────────────────────────────────────┘
                          │
                          ↓
              https://yourdomain.com
                (Custom domain required)
```

**Pros:**
- ✅ Complete control
- ✅ SSH access
- ✅ Custom configurations
- ✅ Fixed monthly cost

**Best for:** Advanced users, learning, multiple projects

---

## 📊 Data Flow

### User Request Flow:

```
User Browser
     │
     │ 1. Request page
     ↓
Frontend (HTML)
     │
     │ 2. Page loads
     │ 3. User searches
     ↓
JavaScript (search.js)
     │
     │ 4. API call: GET /api/projects
     ↓
Backend Server (Express)
     │
     │ 5. Query database
     ↓
PostgreSQL Database
     │
     │ 6. Return data
     ↓
Backend Server
     │
     │ 7. Send JSON response
     ↓
JavaScript
     │
     │ 8. Display results
     ↓
User sees results ✨
```

---

## 🔐 Authentication Flow

```
User (Not Logged In)
     │
     │ 1. Click "Login"
     ↓
Login Page (/login)
     │
     ├─────────────────────┬───────────────────┐
     │                     │                   │
     ↓                     ↓                   ↓
Email/Password      Google OAuth       (Future: GitHub)
     │                     │
     │ 2. Submit           │ 2. Redirect to Google
     ↓                     ↓
Backend Auth Route    Google Auth
(/api/auth/login)     (/api/auth/google)
     │                     │
     │ 3. Verify           │ 3. Verify with Google
     ↓                     ↓
Check Database        Get user info
     │                     │
     │ 4. Create session   │ 4. Create/update user
     └─────────┬───────────┘
               ↓
     Session Created (PostgreSQL)
               │
               │ 5. Set cookie
               ↓
     User Dashboard/Admin Panel
               │
               │ User can now:
               ├─ Upload projects
               ├─ Manage content
               └─ Access admin features
```

---

## 🗄️ Database Schema

```
┌─────────────────────────────────────────────────────────────┐
│                    PostgreSQL Database                      │
│                                                             │
│  ┌──────────────────┐          ┌──────────────────┐        │
│  │     users        │          │   team_members   │        │
│  ├──────────────────┤          ├──────────────────┤        │
│  │ id (PK)          │          │ id (PK)          │        │
│  │ email            │          │ name             │        │
│  │ password_hash    │          │ role             │        │
│  │ google_id        │          │ bio              │        │
│  │ role (admin)     │          │ portfolio_url    │        │
│  │ created_at       │          │ github_url       │        │
│  └──────────────────┘          │ social_links     │        │
│                                │ created_at       │        │
│  ┌──────────────────┐          └──────────────────┘        │
│  │    projects      │                                      │
│  ├──────────────────┤                                      │
│  │ id (PK)          │                                      │
│  │ title            │          ┌──────────────────┐        │
│  │ description      │          │    sessions      │        │
│  │ category         │          ├──────────────────┤        │
│  │ tags (array)     │          │ sid (PK)         │        │
│  │ image_url        │          │ sess (JSON)      │        │
│  │ member_id (FK) ──┼──────────│ expire           │        │
│  │ created_at       │          └──────────────────┘        │
│  └──────────────────┘                                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Deployment Workflow

### Railway Auto-Deploy:

```
Local Development
     │
     │ git commit
     │ git push
     ↓
GitHub Repository
     │
     │ Webhook triggers
     ↓
Railway Platform
     │
     ├─ 1. Pull code
     ├─ 2. Install dependencies (npm install)
     ├─ 3. Build (if needed)
     ├─ 4. Run start command
     └─ 5. Health check
     ↓
Live Deployment ✅
     │
     └─ Rollback if fails ❌
```

### Manual VPS Deploy:

```
Local Development
     │
     │ git push
     ↓
GitHub Repository
     │
     │ SSH into server
     │ git pull
     ↓
VPS Server
     │
     ├─ 1. Pull latest code
     ├─ 2. npm install
     ├─ 3. Run migrations (if any)
     ├─ 4. pm2 restart
     └─ 5. Check logs
     ↓
Live Deployment ✅
```

---

## 🌐 Network Architecture

```
                    Internet
                       │
                       │ HTTPS (443)
                       ↓
                ┌──────────────┐
                │   CloudFlare │  (Optional CDN)
                │   (Optional) │
                └──────┬───────┘
                       │
                       ↓
            ┌─────────────────────┐
            │   Load Balancer     │  (Railway/Cloud)
            │   (Auto-scaling)    │
            └──────┬──────────────┘
                   │
        ┏━━━━━━━━━━┻━━━━━━━━━━┓
        ↓                     ↓
   ┌─────────┐          ┌─────────┐
   │ Server 1│          │ Server 2│  (Auto-scaled)
   └────┬────┘          └────┬────┘
        │                    │
        └────────┬───────────┘
                 ↓
        ┌─────────────────┐
        │    Database     │
        │  (Single/Master)│
        └─────────────────┘
```

---

## 📦 File Storage Options

### Option A: Local Storage (Simple)

```
Backend Server
     │
     └─ /uploads/
         ├─ /projects/
         │   ├─ image1.jpg
         │   └─ image2.png
         └─ /profiles/
             └─ avatar.jpg

Served at: https://yourdomain.com/uploads/image1.jpg
```

### Option B: Cloudinary (Recommended)

```
User Upload
     │
     ↓
Backend Server
     │
     │ Multer processes
     ↓
Cloudinary API
     │
     │ Stores file
     │ Returns URL
     ↓
Database (save URL)
     │
     ↓
https://res.cloudinary.com/your-cloud/image.jpg
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────────────┐
│                    Security Stack                       │
│                                                         │
│  Layer 1: HTTPS/SSL (Encryption in transit)           │
│  └─ Railway: Automatic                                 │
│  └─ VPS: Let's Encrypt (Certbot)                       │
│                                                         │
│  Layer 2: Helmet (Security headers)                    │
│  └─ XSS Protection                                      │
│  └─ CSRF Protection                                     │
│  └─ Content Security Policy                            │
│                                                         │
│  Layer 3: Authentication                               │
│  └─ Passport.js (Local + Google OAuth)                 │
│  └─ Password hashing (bcrypt)                          │
│  └─ Session management                                 │
│                                                         │
│  Layer 4: Authorization                                │
│  └─ Admin middleware                                    │
│  └─ Role-based access                                   │
│                                                         │
│  Layer 5: Rate Limiting                                │
│  └─ Express-rate-limit                                  │
│  └─ 100 requests / 15 minutes                          │
│                                                         │
│  Layer 6: Input Validation                             │
│  └─ Express-validator                                   │
│  └─ SQL injection prevention                            │
│  └─ File upload validation                              │
│                                                         │
│  Layer 7: Database Security                            │
│  └─ Encrypted connection (SSL)                          │
│  └─ Parameterized queries                               │
│  └─ Limited user permissions                            │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 Monitoring Setup

```
Your Application
     │
     ├─ Logs → Railway Dashboard / PM2 logs
     │
     ├─ Errors → Sentry (optional)
     │
     ├─ Uptime → UptimeRobot
     │     │
     │     └─ Pings every 5 minutes
     │         Alerts on downtime
     │
     ├─ Analytics → Google Analytics (optional)
     │
     └─ Performance → New Relic (optional)
```

---

## 🚀 Choose Your Architecture

| Need | Use This |
|------|----------|
| **Quick start** | Railway (Option 1) |
| **Best performance** | Vercel + Railway (Option 2) |
| **Full control** | VPS (Option 3) |
| **Learning** | VPS (Option 3) |
| **Production** | Railway or Split Stack |
| **Free tier** | Railway or Vercel+Railway |

---

**Ready to deploy?** Go to [QUICK_DEPLOY.md](QUICK_DEPLOY.md)

**Need details?** Read [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)

